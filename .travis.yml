---
language:
  - 'python'
python:
  - '2.7'
  - '3.4'
install:
  - 'pip install ansible'
  - 'truncate -s 0 roles/appserver/vars/keys.yml'
  - 'sed -i "" "/ask_vault_pass/d" ansible.cfg'
script:
  - 'ansible-playbook site.yml --syntax-check'